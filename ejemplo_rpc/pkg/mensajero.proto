syntax = "proto3";

package mensajero;

option go_package = "/mensajero/pkg";

message Registracion {
    string usuarioOrigen = 1;
}

message TokenAutenticacion {
    string token = 1;
}

service Mensajero {
    rpc Conectar(Registracion) returns (TokenAutenticacion);
}